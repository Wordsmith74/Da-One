# Copy this file to config/.env for local runs (never commit the real .env).
# In production (GitHub Actions), these are set as repo Secrets instead --
# see SETUP.md step 2.

BALL_DONT_LIE_KEY=
THE_ODDS_API_KEY=

# Optional / referenced in SETUP.md but not required by any current code path
# (data/fetch.py only reads BALL_DONT_LIE_KEY and THE_ODDS_API_KEY today).
# Leave blank unless you've wired a feature that needs them.
PROP_LINE_API_KEY=
RAPIDAPI_KEY=
